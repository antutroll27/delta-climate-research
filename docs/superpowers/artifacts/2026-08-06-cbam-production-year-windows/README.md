SUPERSEDED — do not apply. Kept as the record of what Phase 1 produced.

The change shipped as CBM 397457e (year windows + thresholds) and 95224d0
(OJ source hashes). This patch predates both the thresholds fix and the
hashes, and no longer applies to build-fa-package.py.

To see what actually shipped:  git -C /Volumes/VSTSAMPLES/Projects/CBM show 397457e
